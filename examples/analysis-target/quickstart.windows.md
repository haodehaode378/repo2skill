# Windows Quickstart

Repository: `analysis-target`
Root Directory: `./tests/fixtures/analysis-target`
Package Manager: `pnpm`
Project Type: `vite`

## Environment Variables

Set these variables before running the project if they are relevant:

- `API_URL` from `.env.example` (high)
- `SECRET_TOKEN` from `src/config.ts` (medium)

## Start Command

Run the most likely start command:

```powershell
pnpm dev
```

## Available Scripts

- `dev`: `pnpm dev` (script: `vite`)
- `build`: `pnpm build` (script: `vite build`)
- `test`: `pnpm test` (script: `vitest run`)

## Notes

- This quickstart only uses commands detected from repository evidence.
- Missing install or setup steps mean no explicit evidence was detected for them.
